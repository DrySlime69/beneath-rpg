function setupInventoryScreen(scene){scene.hotbarGrid.innerHTML='';scene.backpackGrid.innerHTML='';scene.dragData=null;for(let i=0;i<8;i++)scene.hotbarGrid.appendChild(createInventorySlot(scene,'hotbar',i));for(let i=0;i<24;i++)scene.backpackGrid.appendChild(createInventorySlot(scene,'backpack',i))}
function createInventorySlot(scene,area,index){const slot=document.createElement('div');slot.className='invSlot';slot.draggable=true;slot.addEventListener('dragstart',e=>{scene.dragData={area,index};slot.classList.add('dragging');e.dataTransfer.setData('text/plain',area+':'+index)});slot.addEventListener('dragend',()=>{slot.classList.remove('dragging');document.querySelectorAll('.invSlot').forEach(s=>s.classList.remove('dropTarget'))});slot.addEventListener('dragover',e=>{e.preventDefault();slot.classList.add('dropTarget')});slot.addEventListener('dragleave',()=>slot.classList.remove('dropTarget'));slot.addEventListener('drop',e=>{e.preventDefault();slot.classList.remove('dropTarget');if(!scene.dragData)return;moveInventoryItem(scene,scene.dragData.area,scene.dragData.index,area,index);scene.dragData=null});return slot}
function getItemArray(scene,area){return area==='hotbar'?scene.hotbarItems:scene.backpackItems}
function moveInventoryItem(scene,fromArea,fromIndex,toArea,toIndex){const a=getItemArray(scene,fromArea),b=getItemArray(scene,toArea);const item=a[fromIndex];a[fromIndex]=b[toIndex];b[toIndex]=item;updateInventoryUI(scene)}
function toggleInventoryScreen(scene){scene.inventoryOpen=!scene.inventoryOpen;scene.inventoryScreen.style.display=scene.inventoryOpen?'flex':'none';updateInventoryUI(scene)}
