function useTeleportStone(scene){if(scene.currentMapName==='mine'){scene.mineReturnPosition={x:scene.player.x,y:scene.player.y};switchToHome(scene);scene.player.x=scene.homePosition.x;scene.player.y=scene.homePosition.y;setMessage(scene,'Teleported home.')}else{switchToMine(scene);scene.player.x=scene.mineReturnPosition.x;scene.player.y=scene.mineReturnPosition.y;setMessage(scene,'Returned to the mine.')}redraw(scene)}
function placeFurnace(scene){let hasFurnaceInHotbar=false;for(const item of scene.hotbarItems){if(item&&item.id==='furnace')hasFurnaceInHotbar=true}if(!scene.hasFurnace||!hasFurnaceInHotbar){setMessage(scene,'Build Furnace and move it to hotbar first.');return}if(scene.currentMapName!=='home'){setMessage(scene,'Furnace can only be placed at home.');return}const px=scene.player.x+scene.lastMoveDirection.x,py=scene.player.y+scene.lastMoveDirection.y;const tile=scene.map[py]?scene.map[py][px]:null;if(!tile||(tile.type!=='homeFloor'&&tile.type!=='teleportPad')){setMessage(scene,'Cannot place Furnace there.');return}for(const object of scene.homePlacedObjects){if(object.x===px&&object.y===py){setMessage(scene,'Something is already there.');return}}scene.homePlacedObjects.push({type:'furnace',x:px,y:py});setMessage(scene,'Placed Furnace.');redraw(scene)}
