function createEmptyMap(scene,width,height){const map=[];for(let y=0;y<height;y++){const row=[];for(let x=0;x<width;x++)row.push({type:'caveWall',hardness:999});map.push(row)}return map}
function generateMaps(scene){scene.mineMap=createMineMap(scene);scene.homeMap=createHomeMap(scene);scene.currentMapName='mine';scene.map=scene.mineMap}
function switchToMine(scene){scene.map=scene.mineMap;scene.currentMapName='mine'}
function switchToHome(scene){scene.map=scene.homeMap;scene.currentMapName='home'}
function darkenColor(color,factor){const r=Math.floor(((color>>16)&255)*factor),g=Math.floor(((color>>8)&255)*factor),b=Math.floor((color&255)*factor);return(r<<16)+(g<<8)+b}
function getTileBaseColor(tile){if(tile.type==='floor')return 0x111111;if(tile.type==='homeFloor')return 0x1a1510;if(tile.type==='teleportPad')return 0x3344aa;if(tile.type==='caveWall')return 0x2a160c;if(tile.type==='exit')return 0x00aa00;if(tile.type==='stone')return tile.hp<=1?0x999999:tile.hp<=2?0x777777:0x555555;if(tile.type==='coal')return tile.hp<=1?0x666666:0x333333;if(tile.type==='copper')return tile.hp<=3?0xffaa66:tile.hp<=6?0xdd8844:0xaa6633;return 0}
function redraw(scene){scene.worldLayer.clear();scene.objectLayer.clear();scene.playerLayer.clear();scene.worldLayer.fillStyle(0x020202);scene.worldLayer.fillRect(0,0,scene.mapWidth*scene.tileSize,scene.mapHeight*scene.tileSize);for(let y=0;y<scene.mapHeight;y++){for(let x=0;x<scene.mapWidth;x++){const tile=scene.map[y][x];let color=getTileBaseColor(tile);const distance=Phaser.Math.Distance.Between(x,y,scene.player.x,scene.player.y);const lightRadius=8.5,minBrightness=.16;const lightStrength=Phaser.Math.Clamp(1-distance/lightRadius,0,1);const brightness=minBrightness+(lightStrength*lightStrength)*.84;color=darkenColor(color,brightness);scene.worldLayer.fillStyle(color);scene.worldLayer.fillRect(x*scene.tileSize,y*scene.tileSize,scene.tileSize-1,scene.tileSize-1)}}const objects=scene.currentMapName==='home'?scene.homePlacedObjects:scene.minePlacedObjects;for(const object of objects){if(object.type==='furnace'){scene.objectLayer.fillStyle(0xff6633);scene.objectLayer.fillRect(object.x*scene.tileSize+4,object.y*scene.tileSize+4,scene.tileSize-8,scene.tileSize-8)}}scene.playerLayer.fillStyle(0xffff66);scene.playerLayer.fillRect(scene.player.x*scene.tileSize+5,scene.player.y*scene.tileSize+5,scene.tileSize-10,scene.tileSize-10);scene.playerLayer.fillStyle(0xffaa00);scene.playerLayer.fillRect(scene.player.x*scene.tileSize+scene.tileSize/2-2+scene.lastMoveDirection.x*7,scene.player.y*scene.tileSize+scene.tileSize/2-2+scene.lastMoveDirection.y*7,4,4)}
