function createHomeMap(scene){const map=createEmptyMap(scene,scene.mapWidth,scene.mapHeight);for(let y=7;y<=9;y++){for(let x=32;x<=34;x++){map[y][x]={type:'homeFloor',hardness:0}}}map[8][33]={type:'teleportPad',hardness:0};for(let y=7;y<=9;y++){for(let x=28;x<=30;x++){map[y][x]={type:'copper',hardness:2,hp:10,maxHp:10}}}map[8][31]={type:'copper',hardness:2,hp:10,maxHp:10};return map}
